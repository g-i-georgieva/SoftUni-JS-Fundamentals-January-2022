function mathPower(num, x) {
    // let result = 1
    // for (let i = 1; i <= x; i++) {
    //     result *=num
    // }
    let result = Math.pow(num, x)
    console.log(result)
}
mathPower(2, 8)