function printPersonInfo(firstName, lastName, age) {
  let personInfo = {
    firstName,
    lastName,
    age,
  };
  return personInfo;
}
console.log(printPersonInfo("Peter", "Pan", "20"));
